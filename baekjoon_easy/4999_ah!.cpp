#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string str1, str2;
    cin >> str1 >> str2;

    if (str1.size() >= str2.size()) cout << 'g';
    else cout << 'n';
    cout << 'o';
}